apiVersion: v1
kind: Service
metadata:
  name: {{ include "sheerwater-benchmarking.fullname" . }}
  labels:
    {{- include "sheerwater-benchmarking.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sheerwater-benchmarking.selectorLabels" . | nindent 4 }}
