apiVersion: v1
kind: Service
metadata:
  name: {{ include "ephemeral-grafana.fullname" . }}
  labels:
    {{- include "ephemeral-grafana.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ephemeral-grafana.selectorLabels" . | nindent 4 }}
