apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ephemeral-grafana.fullname" . }}-test-connection"
  labels:
    {{- include "ephemeral-grafana.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ephemeral-grafana.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
